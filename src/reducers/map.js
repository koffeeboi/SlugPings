import * as types from "../constants/MapConstants";

const initialState = {
};

const map = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default map;